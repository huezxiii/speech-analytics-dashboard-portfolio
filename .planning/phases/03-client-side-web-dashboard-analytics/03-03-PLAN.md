---
phase: "03"
plan: "03"
type: implementation
prefix: "03-03"
title: "Implement NLP Query Sandbox, Proximity Search, and Evaluator"
wave: 2
depends_on: ["03-01"]
---

<objective>
Implement Tab 4: CallMiner NLP Query Sandbox in `index.html` with Boolean and `W/15` proximity query evaluator, real-time Precision/Recall calculations against filtered datasets, snippet keyword highlighting, and hit velocity visualizer.
</objective>

<tasks>
<task id="1">
Implement query evaluation engine in `index.html`:
- Support Boolean operators: `AND`, `OR`, `NOT`, and parenthetical grouping `(...)`.
- Support proximity operator `W/N` (e.g. `"connection" W/15 "update"`).
- Syntax validation (unbalanced parentheses, consecutive operators).
</task>

<task id="2">
Implement query results grid and metrics:
- Precision and Recall estimation against incident categories.
- Real-time result table with search term highlighting in transcript excerpts.
- Query hit velocity chart (`queryVelocityChart`) and keyword proximity distance scatter (`proximityScatterChart`).
</task>
</tasks>

<verification>
- Proximity queries execute without syntax errors.
- Search keyword tokens highlight with yellow/amber badges.
- Precision, Recall, and F1 metrics update dynamically.
</verification>
