---
phase: "03"
plan: "02"
type: implementation
prefix: "03-02"
title: "Implement Chart.js Visualizers, Theme Palettes, and ARIA Data Tables"
wave: 2
depends_on: ["03-01"]
---

<objective>
Implement all secondary chart visualizers across Tabs 1, 2, and 3 in `charts.js`, apply the sanitized Slate/Sky design palette, and populate hidden ARIA-accessible data tables for every visualizer.
</objective>

<tasks>
<task id="1">
Implement `charts.js`:
- Palette: Modern Sky/Slate tokens matching `brand.css`.
- Tab 1: Category Distribution stacked bar (`categoryDistributionChart`), FCR vs. Sentiment bubble matrix (`fcrSentimentMatrixChart`).
- Tab 2: Volume & Silence mix chart (`volumeSilenceChart`), Talk-to-Silence balance doughnut (`talkSilenceDoughnutChart`), Queue efficiency leaderboard table, and Silence duration histogram (`silenceHistogramChart`).
- Tab 3: Policy adherence half-doughnut gauge (`hipaaGaugeChart`), 5-axis agent behavioral radar chart (`agentRadarChart`), interaction agitation curve (`agitationCurveChart`), and linguistic empathy correlation bar chart (`empathyBarChart`).
</task>

<task id="2">
Implement ARIA data tables:
- `populateAria(elementId, summary, headers, rows)` helper generating structured HTML `<table>` elements into hidden companion containers for every chart.
</task>
</tasks>

<verification>
- All canvas IDs match `index.html` elements.
- Chart instances are registered and destroyed cleanly on filter changes.
- Every chart has a matching ARIA screen-reader table container.
</verification>
