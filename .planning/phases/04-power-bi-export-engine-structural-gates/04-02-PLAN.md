---
phase: "04"
plan: "02"
type: implementation
prefix: "04-02"
title: "Implement Power BI Gate Verification Scripts and Test Suite"
wave: 2
depends_on: ["04-01"]
---

<objective>
Port and sanitize the entire suite of Power BI structural gate checkers and unit tests (`check-*.js`, `test-powerbi.js`, `test-index-export.js`), wire them into `package.json`, and verify all checks pass cleanly.
</objective>

<tasks>
<task id="1">
Port and sanitize verification scripts:
- `test-powerbi.js`: Main unit test suite asserting TMDL/PBIR structure, theme palette, DAX measures, and drill-through bindings.
- `check-powerbi-freshness.js` (`check:pbi`): Asserts disk vs. generator byte-parity and PNG signature correctness.
- `check-date-dimension.js` (`check:date`): Asserts DateTable marked, full calendar span, key/cardinality.
- `check-weighted-measures.js` (`check:weighted`): Asserts duration-weighted ratio DAX shape.
- `check-drillthrough.js` (`check:drill`): Asserts drill-through target page bindings and PII disclosure.
- `check-column-consumers.js` (`check:columns`): Asserts all 21 Calls columns are accounted for.
- `check-compliance-page.js` (`check:compliance`): Asserts compliance visual slots and unique logical IDs.
- `check-card-reference-labels.js` (`check:cards`): Asserts card visual reference labels.
- `check-narrative-titles.js` (`check:narrative`): Asserts title measures and zero-row guards.
- `check-pop-measures.js` (`check:pop`): Asserts period-over-period DAX patterns.
- `check-target-lines.js` (`check:target`): Asserts target reference lines and disclosures.
- `test-index-export.js` (`check:export`): Asserts browser export contract compatibility.
</task>

<task id="2">
Wire `package.json`:
- Register all `check:*` commands and `test` command in `package.json`.
</task>

<task id="3">
Execute full gate verification:
- Run `npm test` and all `npm run check:*` scripts to verify 100% green pass.
</task>
</tasks>

<verification>
- `npm test` passes with 0 exit code.
- Every `npm run check:*` script exits with 0.
- `check-powerbi-freshness.js` confirms `powerbi/` on disk is 100% byte-fresh.
</verification>
