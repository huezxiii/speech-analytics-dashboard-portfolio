---
phase: "02"
plan: "01"
type: implementation
prefix: "02-01"
title: "Implement Preprocessor Core, CLI Shell, and Automated Parity Tests"
wave: 1
depends_on: []
---

<objective>
Implement `preprocess_core.py` with zero GUI dependencies, provide `preprocess_cli.py` for headless batch automation, and implement `tests/test_core.py` and `tests/test_parity.py` to ensure 100% test passing and byte-parity verification.
</objective>

<tasks>
<task id="1">
Implement `preprocess_core.py`:
- Pure Python module using only stdlib (`collections`, `csv`, `re`, `itertools`).
- Constants: `REQUIRED_COLUMNS` (15 canonical fields).
- Classes: `ValidateResult`, `PreprocessError`, `MissingColumnsError`.
- Functions: `extract_number()`, `_clean_row()`, `process_file()`, `write_csv()`, `combine()`, `validate()`, `preview()`.
</task>

<task id="2">
Implement `preprocess_cli.py`:
- Command-line interface with arguments for input file/directory, output path, and combine flag.
- Detailed progress and validation feedback to stdout/stderr.
- Clean exit codes (0 for success, 1 for validation or file errors).
</task>

<task id="3">
Implement test suites:
- `tests/test_core.py`: Test unit edge cases (empty strings, missing columns, non-numeric noise, multi-file sorting, and headers).
- `tests/test_parity.py`: Test byte-parity between direct Core processing and CLI execution on synthetic datasets.
</task>
</tasks>

<verification>
- `python -m pytest tests/test_core.py tests/test_parity.py` passes 100%.
- CLI batch test on `dashboard-ready.csv` executes cleanly with code 0.
</verification>
