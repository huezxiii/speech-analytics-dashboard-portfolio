---
phase: "02"
plan: "02"
type: implementation
prefix: "02-02"
title: "Implement pywebview Desktop GUI Shell and PyInstaller Spec"
wave: 2
depends_on: ["02-01"]
---

<objective>
Implement the desktop companion app shell using `pywebview` with JS-Python RPC bridge, a sanitized side-by-side preview with diff cell highlighting, and configure the PyInstaller build specification `build/preprocessor.spec`.
</objective>

<tasks>
<task id="1">
Implement `gui/app.py`:
- `pywebview` window manager with WebView2 detection.
- RPC API bridge class exposing `select_files()`, `select_folder()`, `get_preview(limit)`, `process_files(output_path, combine)`.
- Thread-safe UI updates and error handling.
</task>

<task id="2">
Implement `gui/web/` local frontend:
- `index.html`: Sanitized layout with file picker, options bar, side-by-side diff table container, and progress indicator.
- `styles.css`: Dark mode theme aligning with `brand.css` Slate / Sky aesthetic and green/red diff indicator pills.
- `app.js`: Bridge client invoking window.pywebview.api and rendering tables with diff styling.
</task>

<task id="3">
Create `build/preprocessor.spec`:
- PyInstaller spec targeting single-file `SpeechAnalyticsPreprocessor.exe`.
- Bundles `gui/web` static assets and includes runtime hooks for WebView2.
</task>
</tasks>

<verification>
- `gui/app.py` imports without error.
- All static assets exist in `gui/web/`.
- `build/preprocessor.spec` exists and defines valid build recipe.
- Unit and UI mock tests pass.
</verification>
